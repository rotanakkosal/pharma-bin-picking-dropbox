services:
  dropbox:
    build: .
    image: pharma-bin-dropbox:latest
    container_name: pharma-bin-dropbox
    network_mode: host
    user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
    volumes:
      - ./storage:/app/storage
    environment:
      INFERENCE_URL: "${INFERENCE_URL:-http://127.0.0.1:8100}"
    command: ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "9009"]
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:9009/health', timeout=3).status == 200 else 1)"]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
    restart: unless-stopped
